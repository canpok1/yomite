name: release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/goreleaser.yml
    with:
      goreleaser-args: "release --clean"
    secrets: inherit
